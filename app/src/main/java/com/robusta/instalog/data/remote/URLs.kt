package com.robusta.instalog.data.remote

object URLs {

    private const val PATH_WEATHER = "weather"
    private const val PATH_FORECAST = "forecast"

    const val URL_GET_CURRENT_WEATHER = PATH_WEATHER
    const val URL_FORECAST = PATH_FORECAST
}